const fs=require("fs");let c=fs.readFileSync("src/App.jsx","utf8");const len=c.length;c=c.split(`setSubTab("programs"); setViewingProgram(prog.id);`).join("setViewingProgram(prog.id); setInitialDayIdx(di);");c=c.split(`subTab === "programs"`).join(`subTab === "workout"`);c=c.split(`subTab === "today"`).join(`subTab === "workout"`);c=c.replace(`["today","Today"],["programs","Programs"],["exercises","Exercises"],["history","History"]`,`["workout","Workout"],["exercises","Exercises"],["history","History"]`);c=c.split(`useState("today")`).join(`useState("workout")`);if(!c.includes("initialDayIdx"))c=c.replace("const [showBuilder, setShowBuilder] = useState(false);","const [showBuilder, setShowBuilder] = useState(false);\n  const [initialDayIdx, setInitialDayIdx] = useState(0);");if(!c.includes("initialDayIdx={initialDayIdx}"))c=c.replace("onBack={() => setViewingProgram(null)}","onBack={() => { setViewingProgram(null); setInitialDayIdx(0); }}\n            initialDayIdx={initialDayIdx}");c=c.replace("onProgramEdited })","onProgramEdited, initialDayIdx = 0 })");c=c.replace("const [expandedDay, setExpandedDay] = useState(0)","const [expandedDay, setExpandedDay] = useState(initialDayIdx)");c=c.replace("const [activeDay, setActiveDay] = useState(0)","const [activeDay, setActiveDay] = useState(initialDayIdx)");fs.writeFileSync("src/App.jsx",c,"utf8");console.log("Was:"+len+" Now:"+c.length);console.log("Edit:"+c.includes("setInitialDayIdx(di)"));console.log("Build:"+c.includes("showBuilder"));
